extends layout

block content
  h1 #{title}
  form(method='POST', action='/bands/add')
    #form-group
      label Name:
      input.form-control(name='name', type='text')
    #form-group
      label Genre:
      input.form-control(name='genre', type='text')
    br
    input.btn.btn-primary(type='submit', value='Submit')
